id: pdf-export
title: Export document as PDF
status: UNKNOWN
preconditions:
- document-in-library(clean-a4)
fixture:
  camera: null
  sequence: null
steps:
- launch
- open-document(clean-a4)
- 'export: pdf'
assertions:
  behavior:
  - export-succeeds
  - exported-file-shared-or-saved
  ui:
  - export-menu-visible
  state: []
  output:
  - pdf-created
  - pdf-readable
meta:
  requires:
  - gui
  - adb
  owner: lead
  notes: Provisional spec from the mission's starting list — pending reference discovery
    (Worker 2). Status stays UNKNOWN until observed against CamScanner; actual reference
    behavior may differ.
