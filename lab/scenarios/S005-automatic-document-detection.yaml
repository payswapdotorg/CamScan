# migrated to DSL v0.1 (typed meta.requires + explicit timeouts); previous requires: ['gui', 'adb', 'camera_fixture']
id: automatic-document-detection
title: Automatic document edge detection
status: UNKNOWN
preconditions:
- camera-permission-granted
fixture:
  camera: clean-a4
steps:
- launch
- 'tap: scan'
- observe-detection-overlay
- capture
assertions:
  behavior:
  - document-edges-detected-automatically
  - detection-overlay-shown-live
  ui:
  - detection-overlay-visible
  state: []
  output: []
meta:
  timeout_seconds: 2400
  step_timeout_seconds: 180
  requires:
    gui: true
    adb: true
    android_emulator: true
    emulator_acceleration:
      allowed:
      - none
    camera_fixture: true
  owner: lead
  notes: Provisional spec from the mission's starting list — pending reference discovery
    (Worker 2). Status stays UNKNOWN until observed against CamScanner; actual reference
    behavior may differ.
