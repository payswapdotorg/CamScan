# migrated to DSL v0.1 (typed meta.requires + explicit timeouts); previous requires: ['gui', 'adb']
id: jpg-export
title: Export page as JPG
status: UNKNOWN
preconditions:
- document-in-library(clean-a4)
fixture:
  camera: null
  sequence: null
steps:
- launch
- open-document(clean-a4)
- 'export: jpg'
assertions:
  behavior:
  - export-succeeds
  - exported-image-viewable
  ui:
  - export-menu-visible
  state: []
  output:
  - jpg-created
  - jpg-readable
meta:
  timeout_seconds: 1800
  step_timeout_seconds: 120
  requires:
    gui: true
    adb: true
    android_emulator: true
    android_sdk: true
    android_cli: true
    emulator_acceleration:
      allowed:
      - none
  owner: lead
  notes: Provisional spec from the mission's starting list — pending reference discovery
    (Worker 2). Status stays UNKNOWN until observed against CamScanner; actual reference
    behavior may differ.
