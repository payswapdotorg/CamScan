[pytest]
# lab-cli unit suite — hermetic: no network, no device, no credentials,
# no wall-clock stamps. The live e2b driver is NEVER exercised here
# (RecordingDriver / --plan only); the lab exercises it on the substrate
# station where E2B_API_KEY lives.
